{
  "name": "ubuntu_setup_dev",
  "version": "1.0.0",
  "description": "ubuntu setup develop enviroment",
  "main": "index.js",
  "scripts": {
    "init": "honkit init",
    "start": "npx honkit serve",
    "build": "honkit build",
    "publish": "gh-pages -d _book -m 'Auto publish book'"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "honkit": "^4.0.0"
  }
}
